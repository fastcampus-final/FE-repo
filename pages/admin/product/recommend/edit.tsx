import withAuth from '@/components/common/PrivateRouter';
import React from 'react';

const ProductRecommendEditForm = () => {
  return <div>ProductRecommendEditForm</div>;
};

export default withAuth(ProductRecommendEditForm);
