import withAuth from '@/components/common/PrivateRouter';
import React from 'react';

const ProductPopularEditForm = () => {
  return <div>ProductPopularEditForm</div>;
};

export default withAuth(ProductPopularEditForm);
