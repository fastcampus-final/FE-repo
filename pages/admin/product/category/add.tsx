import withAuth from '@/components/common/PrivateRouter';
import React from 'react';

const ProductCetegoryAddForm = () => {
  return <div>ProductCetegoryAddForm</div>;
};

export default withAuth(ProductCetegoryAddForm);
