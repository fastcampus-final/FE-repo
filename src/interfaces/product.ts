export interface IProduct {
  productId: string;
  title: string;
  price: string;
  imagePath: string;
}

export interface IDetail {
  productId: string;
  title: string;
  price: string;
  imagePath: string;
  summary: string;
  area: string;
  point: string;
  airline: string;
}
