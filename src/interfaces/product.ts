export interface IProduct {
  productId: string;
  productName: string;
  productPrice: string;
  productThumbnail: string;
}

export interface IProductDetail {
  productId: string;
  productName: string;
  productPrice: string;
  productThumbnail: string;
  // summary: string;
  // area: string;
  // point: string;
  // airline: string;
}
